export interface createTransactionProps{
    amount: number,
    description: string,
} 

export interface objectHistory{
    amount: number,
    date: string,
    description: string,
    _id: string
}
